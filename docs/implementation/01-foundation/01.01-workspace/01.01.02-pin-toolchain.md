# Task 01.01.02: Pin toolchain and dependencies

Configure stable Rust, Bevy 0.19 with its 2D profile, and explicit versions for serialization, networking, persistence, error handling, IDs, and test tooling.

## Status

- [x] Done

## Implementation notes

- Pinned Rust 1.95, Bevy 0.19's minimum supported Rust version, and committed `Cargo.lock`.
- Bevy uses `default-features = false` with the `2d`, `x11`, and `wayland` features; no 3D profile is enabled.
- Root documentation lists native build prerequisites for Linux, Windows, and macOS.
- Dependency versions and distributable project licensing are declared in the workspace manifest.

## Dependencies

- 01.01.01.

## Acceptance criteria

- A fresh checkout selects the documented toolchain and produces a committed lockfile.
- Bevy excludes unused 3D features.
- Dependency licenses are compatible with distribution.
- Developer documentation lists required native packages for each supported OS.

## Cross-cutting concerns

- Reproducibility, supply-chain review, and compile time.
