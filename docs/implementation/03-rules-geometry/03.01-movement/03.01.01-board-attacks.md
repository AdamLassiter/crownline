# Task 03.01.01: Implement board queries and attack maps

Create bounded coordinate operations, occupancy queries, rays, jumps, and geometric attack maps.

## Status

- [x] Done

## Implementation notes

- Bounded checked offsets, occupancy, terrain/edge-aware rays, jumps, and geometric attack detection are implemented in the shared rules module.
- Public `attack_lines_on` queries return stable attacker identities and attacker-to-target paths without applying King-safety filtering.
- Friendly-occupied endpoints are reported as protected while occupancy, Forest, Mountain, river, and wall barriers stop rays consistently with movement geometry.
- Focused table-style tests cover a corner Knight, edge rays, friendly blockers, out-of-bounds targets, and both configured Pawn orientations.

## Dependencies

- 02.02.01.

## Acceptance criteria

- Coordinate operations cannot wrap or escape the board.
- Rays stop at the first relevant blocker and include protected friendly-occupied endpoints.
- Attack queries identify attacker IDs and lines, not only a Boolean.
- Table tests cover corners, edges, blockers, and both player orientations.

## Cross-cutting concerns

- Avoid allocation in inner ray traversal where practical.
