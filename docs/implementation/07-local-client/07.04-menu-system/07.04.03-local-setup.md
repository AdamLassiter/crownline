# Task 07.04.03: Replace local setup hotkeys

Build visible Scenario, Players, and Time Control forms and close Bug 023 by
routing controls and retained accelerators through the same setup actions.

## Status

- [x] Done

## Implementation notes

- Added visible Scenario, Players, and Time Control sections using authored
  scenario metadata, editable names, controller selectors, clock toggles and
  bounded steppers, side swapping, inline validation, Back, and Start controls.
- The menu calls the existing validated local match constructor; no duplicate
  setup or rules path was introduced.
- Local setup accelerators and pointer controls produce the same typed actions.
  Letter and punctuation accelerators are suppressed during text editing while
  function/navigation keys remain available.
- The legacy lifecycle hotkey handler is gated whenever the menu owns input, so
  an action cannot be applied twice.
- Regression coverage exercises the Bug 023 keys and text-focus boundary.
- Verified with the menu and lifecycle tests and Clippy with warnings denied.

## Dependencies

- 07.04.02, 07.03.01, 07.03.02, 13.02.03.

## Acceptance criteria

- Scenario, side swap, both controllers, clock enablement, base time,
  increment, and Start work through pointer and keyboard controls.
- Advertised accelerators invoke identical actions outside active text editing;
  character accelerators are suppressed while an editable field owns focus.
- Invalid names and AI/fog combinations disable Start and identify the affected
  field.
- Starting produces the same validated revision-zero canonical state as the
  existing flow.
- Regression tests reproduce Bug 023 for `X`, `F2`, `C`, `-`, `+`, scenario,
  and controller controls.

## Cross-cutting concerns

- Input parity, validation, focus ownership, and deterministic match creation.
