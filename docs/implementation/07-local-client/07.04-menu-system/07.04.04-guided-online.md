# Task 07.04.04: Integrate Guided and Online flows

Restyle the guided browser and Online Host, Join, Waiting, and reconnect flows
using the shared shell and controls.

## Status

- [x] Done

## Implementation notes

- Home now hands Guided Play to the existing bounded tutorial/challenge browser
  through an explicit runtime API; closing the browser restores Home rather
  than exposing the legacy setup surface.
- Rules & Legend now opens the existing scenario-aware Help presentation
  directly from its Home control.
- Online Play enters the existing protocol-authoritative lobby through an
  explicit menu API and returns to Home when the lobby closes.
- Added visible Host, Join, Back, scenario, clock, Create, Join, invitation,
  Ready, and Leave buttons. Controls are filtered by lobby sub-screen and
  pending requests remain locked by the existing transport boundary.
- Existing guided progress, reconnect, invitation redaction, safe server errors,
  and authoritative-snapshot tests remain the behavioral source of truth.
- Focused Guided, Online, and menu suites plus Clippy with warnings denied pass.

## Dependencies

- 07.04.02, 09.01.01, 09.01.02, 14.01.02.

## Acceptance criteria

- Guided scenarios retain progress, resume/reset, hint, retry, and completion
  behavior.
- Host and Join validate visible fields and cannot issue duplicate pending
  requests.
- Waiting-room invitations never expose reconnect credentials.
- Online gameplay begins only from an authoritative started snapshot.
- Every action remains usable without a shortcut.

## Cross-cutting concerns

- Async feedback, protocol authority, credential privacy, and safe errors.
