# Task 07.04.07: Complete accessibility and integration validation

Audit every route, remove obsolete hotkey-only copy, update player-facing
documentation, and complete Story 07.04 and Epic 07.

## Status

- [x] Done

## Implementation notes

- Disabled and pending shared controls now receive a negative Bevy `TabIndex`
  and relinquish stale focus. Enabled controls retain stable authored order,
  and focused controls use a persistent outline independent of hue.
- Added headless regressions for disabled focus handling, modal precedence,
  editable-field accelerator ownership, pointer/focused-key action parity,
  local-versus-online pause semantics, and presentation-only canonical hash
  preservation.
- Replaced remaining player-facing hotkey-first help, persistence, lobby, and
  online lifecycle copy with control-first wording; retained shortcuts are
  shown as optional accelerators. Updated the player guide around Home,
  categories, transactional Settings, and menu-owned save/load.
- Required native checks rendered the Home menu in isolated X11 displays at
  exactly 800x480 and 1280x800. The minimum viewport keeps its footer visible
  and scrolls the route content; the desktop viewport exposes the complete
  route without overlap.
- Live validation found and resolved [Bug 024](../../bugs/024-online-ui-query-conflict.md),
  a Bevy mutable-query conflict introduced with the online GUI surfaces.
- Formatting, workspace Clippy with warnings denied, and
  `./scripts/check.sh` pass. The client suite reports 147 passed and one
  scheduled benchmark ignored; all workspace suites pass.

## Dependencies

- 07.04.01 through 07.04.06, 10.03.02.

## Acceptance criteria

- Every action is pointer-accessible and keyboard-focusable with visible focus.
- Hidden and disabled controls are skipped; active text editing does not trigger
  character accelerators.
- Menu overlays capture pointer and gameplay commands from the board beneath.
- Local pause, online clock continuation, settings rollback, save confirmation,
  and modal precedence have headless regressions.
- Live rendering is checked at 800x480 and a representative desktop resolution.
- Formatting, Clippy with warnings denied, and `./scripts/check.sh` pass.

## Cross-cutting concerns

- Accessibility evidence, regression coverage, documentation accuracy, and
  release-gate compatibility.
