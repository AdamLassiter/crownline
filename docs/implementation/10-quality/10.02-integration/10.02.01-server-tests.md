# Task 10.02.01: Test server concurrency and recovery

Drive HTTP/WebSocket clients against an ephemeral server and SQLite database.

## Status

- [x] Done

## Implementation notes

- Added a real-loopback integration harness that binds an ephemeral Axum server to an isolated uniquely named SQLite database, drives room creation/join over HTTP, and authenticates multiple Tokio Tungstenite clients over WebSocket with bounded three-second receives.
- The two-seat path covers ready/start, an accepted Hold, exact duplicate replay, two independently keyed same-revision commands submitted concurrently, stale rejection, disconnect, full process/router/database restart, credential reauthentication, draw offer/accept, terminal state, two-seat rematch, fresh match identity/state, and resignation in the rematch.
- The room uses a real one-minute clock. Restart preserves revision/hash/state, and the first post-restart command proves the persisted deadline charges downtime. Existing exact-deadline authority regressions cover timeout winning at the receipt boundary without making the integration suite sleep for a minute.
- Malformed authenticated input and an invalid credential are automatic public-error regressions. Every post-authentication server frame is checked against both raw reconnect tokens, while debug formatting is also checked for redaction.
- Existing repository/actor suites continue to cover atomic rollback, crash containment, queue saturation, terminal durability, failed-publish isolation, and exact duplicate/stale semantics below the socket boundary.
- Verification: the loopback lifecycle test, all workspace tests, and strict all-target clippy pass.

## Dependencies

- Epic 08.

## Acceptance criteria

- Tests cover create/join/ready, full actions, disconnect/reconnect, draw, resign, timeout, terminal, and rematch.
- Concurrent same-revision commands commit once.
- Duplicate idempotency requests return the original result.
- Process restart restores state/hash/revision and effective deadline.

## Cross-cutting concerns

- Assert that responses/log captures do not leak token values.
