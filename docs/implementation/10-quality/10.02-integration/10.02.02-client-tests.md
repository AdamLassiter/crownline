# Task 10.02.02: Test headless client projection and sync

Test board-coordinate mapping, selection, overlays, pending states, snapshot reconciliation, and reconnect using headless Bevy apps and mock transport.

## Status

- [x] Done

## Implementation notes

- Consolidated the existing headless Bevy coverage for board spawning/rebuilding, stable piece projection, coordinate round-trips in both orientations, selection/reselection/cancellation, legal hover previews, overlay precedence, pending online intent locking, and full local interaction/render plugin updates.
- Expanded fit-scale coverage across all 16x16, 20x20, and 24x24 authored boards at 800x600, 1280x720, 1920x1080, and 2560x1440. Existing coordinate tests cover every representative corner, negative space, borders, labels, and world/board round-trip.
- Added a focused UI-capture regression at the input reducer boundary: the same left click activates a hovered square when uncaptured and produces no board activation when the UI owns the pointer.
- Added authoritative reconnect cases for command, promotion choice, produced-Pawn placement, outstanding draw, check, and terminal state. Each validates the protocol snapshot and scenario hash, replaces the complete canonical resource, and confirms exact phase/state reconstruction.
- Existing synchronization regressions prove stale/older snapshots cannot roll back state, equal-revision divergence forces resync, accepted replacement clears transition/notices and invalid context, and a locked online intent cannot duplicate-submit or mutate canonical state before acknowledgement.
- Verification: all headless client tests, the workspace suite, and strict all-target clippy pass.

## Dependencies

- Epic 09, 06.02.01.

## Acceptance criteria

- Tests cover all supported resolutions/scales and board corners.
- Rejected/stale commands restore authoritative state without duplicate submission.
- Reconnect reconstructs promotion, placement, draw, check, and terminal UI.
- UI-captured input never submits a board command.

## Cross-cutting concerns

- Keep transport and renderer mocks behaviorally faithful to public interfaces.
