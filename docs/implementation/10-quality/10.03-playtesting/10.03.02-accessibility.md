# Task 10.03.02: Audit readability and accessibility

Evaluate board and menus under color-vision simulation, UI scaling, keyboard-only input, reduced motion, and common resolutions.

## Status

- [x] Done

## Implementation notes

- Added `docs/accessibility-audit.md` with the reproducible non-hue, keyboard, scaled-layout, common-resolution, and reduced-motion evidence matrix. The audit uses semantic style/entity inspection and monochrome luminance simulation so GPU/font rasterization does not make the gate unstable.
- Added compact `F`/`M`/`R` marks to non-Open terrain and `N`/`S` marks to Keep tiles. Existing piece plates retain contrast plus owner-specific rotation; settlements retain `-`/`N`/`S` plus rotation; all 12 overlay meanings now have a regression proving unique symbols. Panels continue to name ownership, progress fractions, readiness, blockers, check, phase, and clock warnings as text.
- Exposed keyboard navigation for every help section with `1`-`5` and a visibly selected background/heading. Existing setup, online lobby, board focus, mandatory choice, match lifecycle, save/load, panel, confirmation, and pointer-capture tests provide the rest of the keyboard-only evidence.
- Applied configured UI scaling through Bug 008, required at least 800x480 logical UI pixels for any window/scale pair, made lifecycle/lobby/panel surfaces vertically scrollable rather than clipped, and recorded an audit matrix from 640x480 at 0.75 through 2560x1440 at 2.5. Existing camera tests cover every authored board at four common resolutions.
- Added a combined reduced-motion regression proving piece interpolation and retirement ghosts are absent while ordered establishment, Pawn-production, and turn-start notices remain visible and durable. Camera controls remain direct and animation-free.

## Dependencies

- 07.02, 06.03.

## Acceptance criteria

- Ownership, terrain, sites, legal moves, check, and progress remain distinguishable without hue.
- All match/menu actions are keyboard reachable with visible focus.
- Text remains readable at configured UI scales without clipping critical controls.
- Reduced motion removes interpolation while retaining ordered event feedback.

## Cross-cutting concerns

- Accessibility regressions become automated image/state tests where stable.
