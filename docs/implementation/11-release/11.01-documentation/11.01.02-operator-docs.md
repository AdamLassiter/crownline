# Task 11.01.02: Write server operations documentation

Document container configuration, TLS proxy expectations, persistence volume, health checks, logging, capacity, backup/restore, migration, and graceful shutdown.

## Status

- [x] Done

## Implementation notes

- Consolidated the release operator path in `docs/server-operations.md`, including an immutable image-and-digest launch example, the source-checkout Compose alternative, loopback binding, TLS/WebSocket reverse-proxy expectations, non-root volume ownership, stdout logging, readiness/liveness routing, and bounded SIGTERM drain behavior.
- Documented every supported server environment variable with its image default, positive-integer validation, SQLite durability choice, and conservative capacity guidance tied to the reproducible soak workload.
- Documented WAL-safe online backup, integrity verification, restore, forward migration, pre-upgrade rollback points, health/reconnect validation, and schema-safe rollback. Task 10.04.02 verifies the backup path against simultaneous active and completed matches and restores an authenticated active match with the exact canonical hash.
- Added security guidance for TLS, reconnect-token secrecy, hashed credential and display-name handling in backups, rate/connection limits, UID/GID 10001 file permissions, log access, and avoiding concurrent old/new containers on one SQLite volume.
- The examples use placeholder release coordinates, synthetic hostnames, loopback publication, and no credentials or cloud-specific services. The final image coordinates and digest are supplied by Task 11.02.02 release notes.
- Rebuilt the locked production image from a clean Docker context, launched it with the documented persistent-volume, public-URL, logging, port, init, and stop-timeout settings, and verified UID/GID 10001, mode-0700 storage, an HTTP-200 ready database, Docker health, SQLite WAL files, and logged graceful SIGTERM completion. The temporary container, volume, and image tag were removed after verification.

## Dependencies

- 08.04.03, 10.04.02.

## Acceptance criteria

- A new operator can launch a persistent server from the published image.
- Backup and restore steps are verified on a database with active and completed matches.
- Upgrade procedure includes compatibility check and rollback point.
- Security section covers token secrecy, rate limits, TLS, file permissions, and log handling.

## Cross-cutting concerns

- Avoid prescribing a specific cloud provider.
